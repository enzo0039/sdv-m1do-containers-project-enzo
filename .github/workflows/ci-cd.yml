name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: ./sdv-api    # Met à jour le contexte
          file: ./sdv-api/Dockerfile  # Spécifie le chemin correct vers le Dockerfile
          push: true
          tags: your-dockerhub-username/sdv-m1do-containers-project-enzo:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Build and push Docker images
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: your-dockerhub-username/sdv-m1do-containers-project-enzo:latest

      - name: Deploy to server (optional)
        run: |
          echo "Deploying to the server..."
          # Ajoute tes commandes de déploiement ici
